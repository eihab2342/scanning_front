// Placeholder only — the "FutureVerify" test console's name/identity is
// explicitly NOT the product's real brand (per project notes: the user has
// their own design to bring later). Swap this one constant once that's in.
export const APP_NAME = "Scanning Engine";
